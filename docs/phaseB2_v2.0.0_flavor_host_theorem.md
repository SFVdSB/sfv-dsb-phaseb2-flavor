# Phase B2 v2.0.0 - Frozen Flavor-HOST Theorem

## Conditional theorem

Let B be the selected regular O(4) Euclidean nucleation saddle. Assume:

1. the frozen Phase-B2 Pati-Salam seed/auxiliary response sector;
2. a three-family permutation carrier whose primitive Higgs channel is the unique collective singlet;
3. the Phase-B2 response coordinates are global reduced matching variables of the one-bounce functional.

Then the primitive family tensor is

`P1 = (1/3) 1 1^T`.

For `A in {Q,u,d}`, let

`DA(r)=diag(fA1(r),fA2(r),fA3(r))`

be the frozen O(4)-conditioned Phase-B2 response matrix. The one-bounce kernels are

`Ku[B] = Nu integral dr H(r) DQ(r) P1 Du(r)`

`Kd[B] = Nd integral dr H(r) DQ(r) P1 Dd(r)`.

The bounce-center collective-coordinate integral restores ordinary four-dimensional translation invariance. Thus the radial integral is a 4D form factor and does not require an independent fifth dimension.

At each radius the integrand is rank one. Because the family response vectors vary with radius, the integral can be rank three. Since `Du(r) != Dd(r)`, the two left Hermitian products need not commute, allowing CKM mixing.

The reduced response saddle is

`A* = Sigma_B`, `B* = -Sigma_B`.

After global reduced matching, these values enter the intrinsic 3+1 daughter EFT as coupling data rather than local dynamical fields.

## Frozen status

**CONDITIONAL EFFECTIVE THEOREM - CLOSED AND FROZEN.**

No new continuous flavor fit parameter, propagating field, or spacetime dimension is introduced by the v2 correction.
